import usePageTitle from "../../hooks/usePageTitle";

function Orders() {
  usePageTitle("Orders");
  return (
    <div>
      <h2>Welcome to Orders page</h2>
    </div>
  );
}

export default Orders;
