

CREATE TABLE menus (
  id SERIAL PRIMARY KEY,
  name VARCHAR(100),
  path VARCHAR(255),
  icon VARCHAR(100),
  permission VARCHAR(100)
);
INSERT INTO menus (name, path, icon, permission) VALUES
('Dashboard', '/admin/dashboard', 'dashboard', 'VIEW_ADMIN_DASHBOARD'),
('Users', '/users', 'users', 'user_read'),
('Create User', '/users/create', 'add', 'user_create'),
('Profile', '/user/dashboard', 'user', 'VIEW_USER_DASHBOARD');

SELECT * FROM user_logins
ORDER BY id DESC;

UPDATE user_logins
SET token_expiry = NOW() + INTERVAL '1 day'
WHERE id = 11;

UPDATE user_logins
SET status =true
WHERE id = 11;

SELECT * FROM home_banners
ORDER BY id ASC 

TRUNCATE TABLE public.home_banners RESTART IDENTITY;

UPDATE home_banners SET image ="slider1_1778068688653.jpg" where id =1

SELECT column_name, data_type
FROM information_schema.columns
WHERE table_name = 'home_banners';

SELECT * FROM roles
ORDER BY id ASC 

SELECT * FROM users
ORDER BY id ASC 
SELECT * FROM user_logins
ORDER BY id ASC ;

ALTER TABLE users 
ADD COLUMN ip_address VARCHAR(45) DEFAULT NULL AFTER status;
ALTER TABLE users 
DROP COLUMN ip_address;

ALTER TABLE users RENAME TO users_old1;
CREATE TABLE users (
  id SERIAL PRIMARY KEY,
  name VARCHAR(100) NUll,
  email VARCHAR(100) UNIQUE NUll,
  mobile VARCHAR(15) UNIQUE NULL,
  role_id INT,
  status BOOLEAN DEFAULT true,
  ip_address varchar(45),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, 
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE home_banners (
  id SERIAL PRIMARY KEY,
  name VARCHAR(100) NULL,
  slug VARCHAR(100) UNIQUE NULL,
  image TEXT,      
  url TEXT,
  description TEXT,          
  status BOOLEAN DEFAULT true,
  is_deleted BOOLEAN DEFAULT false,
  deleted_at TIMESTAMP NULL,
  ip_address VARCHAR(45),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, 
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

SELECT * FROM user_logins
ORDER BY id ASC ;
ALTER TABLE user_logins RENAME TO user_logins_old21;



CREATE TABLE user_logins (
  id SERIAL PRIMARY KEY,
  user_id INT REFERENCES users(id) ON DELETE CASCADE,
  email VARCHAR(100) UNIQUE,
  password TEXT,
  mobile VARCHAR(15) UNIQUE,
  token VARCHAR(128),
  status BOOLEAN DEFAULT false,
  ip_address VARCHAR(45),
  last_login TIMESTAMP,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

DROP TABLE IF EXISTS user_logins CASCADE;
DROP TABLE IF EXISTS users CASCADE;

CREATE TABLE users (
  id SERIAL PRIMARY KEY,
  name VARCHAR(100),
  email VARCHAR(100) UNIQUE,
  mobile VARCHAR(15) UNIQUE,
  role_id INT NOT NULL,
  status BOOLEAN DEFAULT true,
  ip_address VARCHAR(45),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
UPDATE user_logins SET status =true where id=1;




CREATE TABLE users (
  id SERIAL PRIMARY KEY,
  name VARCHAR(100),
  email VARCHAR(100) UNIQUE,
  mobile VARCHAR(15) UNIQUE,
  role_id INT,
  status BOOLEAN DEFAULT true,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, 
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE user_logins (
  id SERIAL PRIMARY KEY,
  user_id INT REFERENCES users(id) ON DELETE CASCADE,
  password TEXT,
  mobile VARCHAR(15) UNIQUE,
  last_login TIMESTAMP
);

CREATE TABLE otps (
  id SERIAL PRIMARY KEY,
  mobile VARCHAR(15),
  otp VARCHAR(6),
  expires_at TIMESTAMP
);

-- Roles
INSERT INTO roles(name) VALUES ('admin'), ('user');

-- Permissions
INSERT INTO permissions(name) VALUES
('user_create'),
('user_read'),
('user_update'),
('user_delete');

-- Assign all permissions to admin
INSERT INTO role_permissions(role_id, permission_id)
SELECT 1, id FROM permissions;

-- Assign limited permissions to user
INSERT INTO role_permissions(role_id, permission_id)
VALUES (2, 2); -- only read

ALTER TABLE roles ADD CONSTRAINT unique_role_name UNIQUE (name);
ALTER TABLE roles 
ADD COLUMN is_deleted BOOLEAN DEFAULT FALSE,
ADD COLUMN deleted_at TIMESTAMP NULL;

SELECT * FROM role_permissions  ORDER BY id DESC;
ALTER TABLE role_permissions ADD COLUMN id SERIAL PRIMARY KEY;
INSERT INTO permissions(name) VALUES
('manage_role')

TRUNCATE TABLE role_permissions CASCADE;

CREATE TABLE role_permissions (
    id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    role_id INTEGER NOT NULL,
    permission_id INTEGER NOT NULL,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
    
    -- Ensure a role cannot have the same permission twice
    CONSTRAINT unique_role_permission UNIQUE (role_id, permission_id),
    
    -- Foreign keys (assuming you have 'roles' and 'permissions' tables)
    CONSTRAINT fk_role FOREIGN KEY (role_id) REFERENCES roles(id) ON DELETE CASCADE,
    CONSTRAINT fk_permission FOREIGN KEY (permission_id) REFERENCES permissions(id) ON DELETE CASCADE
);
INSERT INTO role_permissions(role_id, permission_id)
SELECT 1, id FROM permissions;

-- Assign limited permissions to user
INSERT INTO role_permissions(role_id, permission_id)
VALUES (2, 2); -- only read



SELECT * FROM user_logins
CREATE TABLE users (
  id SERIAL PRIMARY KEY,
  name VARCHAR(100) NOT NULL,
  email VARCHAR(100) UNIQUE NOT NULL,
  mobile VARCHAR(15) UNIQUE,
  role_id INT NOT NULL,
  status BOOLEAN DEFAULT true,
  is_deleted BOOLEAN DEFAULT false,
  deleted_at TIMESTAMP NULL,
  ip_address VARCHAR(45) NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE user_logins (
  id SERIAL PRIMARY KEY,
  user_id INT NOT NULL UNIQUE
    REFERENCES users(id) ON DELETE CASCADE,
  password TEXT, 
  pwd_generated_at TIMESTAMP NULL,
  token VARCHAR(255), 
  token_expiry TIMESTAMP NULL,
  status BOOLEAN DEFAULT false,
  is_deleted BOOLEAN DEFAULT false,
  deleted_at TIMESTAMP NULL,
  last_login TIMESTAMP NULL,
  ip_address VARCHAR(45) NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

SELECT * FROM roles
ORDER BY id ASC 

SELECT * FROM users
ORDER BY id ASC 

SELECT * FROM user_logins
ORDER BY id ASC ;
TRUNCATE TABLE users, user_logins RESTART IDENTITY CASCADE;

UPDATE user_logins 
SET token = 'df3a37e18327ffef3e673f4d1bc1759134cd3b9a57fbe77bd13de36c17b99fa7'
WHERE id = 2;

UPDATE user_logins 
SET token ='5f1356cbb5d77d4e4d0b7d9e383020c4060561c21b15869a4727d6939dbb84aa' where id=3

ALTER TABLE user_logins 
DROP pwd_genarete_at;


CREATE TABLE users_backup AS SELECT * FROM users;
CREATE TABLE user_logins_backup AS SELECT * FROM user_logins;

UPDATE user_logins
SET token_expiry = NOW() + INTERVAL '1 day'
WHERE id = 1;

UPDATE user_logins
SET status = false
WHERE id = 1;

ALTER TABLE user_logins 
DROP pwd_generate_at


SELECT ul.*
FROM user_logins ul
LEFT JOIN users u ON u.id = ul.user_id
WHERE u.id IS NULL;  

DELETE FROM user_logins
WHERE user_id IS NULL;
DELETE FROM user_logins
WHERE user_id NOT IN (SELECT id FROM users);

UPDATE user_logins
SET user_id = 1
WHERE id = 10;
ALTER TABLE user_logins
ALTER COLUMN user_id SET NOT NULL;

ALTER TABLE user_logins
ADD CONSTRAINT fk_user_login
FOREIGN KEY (user_id)
REFERENCES users(id)
ON DELETE CASCADE;

SELECT ul.email, u.email
FROM user_logins ul
JOIN users u ON u.id = ul.user_id;

SELECT COUNT(*) FROM user_logins WHERE user_id IS NULL;

ALTER TABLE user_logins
ALTER COLUMN user_id SET NOT NULL;

ALTER TABLE user_logins
ADD CONSTRAINT fk_user_login_user
FOREIGN KEY (user_id)
REFERENCES users(id)
ON DELETE CASCADE;

SELECT *
FROM user_logins ul
LEFT JOIN users u ON u.id = ul.user_id
WHERE u.id IS NULL;

DELETE FROM user_logins
WHERE user_id NOT IN (SELECT id FROM users);

D:\PostgreSQL\18\bin>pg_dump -U postgres -h localhost -d beefresh_honey2026 > D:\backup_file.sql
Password:
SELECT COUNT(*) FROM user_logins WHERE user_id NOT IN (SELECT id FROM users);

CREATE INDEX idx_user_logins_user_id ON user_logins(user_id);


SELECT * FROM public.users;

CREATE TABLE users (
  id SERIAL PRIMARY KEY,
  name VARCHAR(100) NOT NULL,
  email VARCHAR(100) UNIQUE NOT NULL,
  mobile VARCHAR(15) UNIQUE,
  role_id INT NOT NULL,
  status BOOLEAN DEFAULT true,
  is_deleted BOOLEAN DEFAULT false,
  deleted_at TIMESTAMP NULL,
  ip_address VARCHAR(45) NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

SELECT * FROM public.role_permissions
ORDER BY id ASC 
DELETE FROM role_permissions;

CREATE TABLE role_permissions (
    id SERIAL PRIMARY KEY,
    role_id INT REFERENCES roles(id),
    permission_id INT REFERENCES permissions(id),
    is_deleted BOOLEAN DEFAULT false,
    deleted_at TIMESTAMP,
    -- This ensures a role can't have the same permission twice
    CONSTRAINT unique_role_permission UNIQUE (role_id, permission_id) 
);
INSERT INTO role_permissions (role_id, permission_id) VALUES
(1,1),
(1,2),
(1,3),
(1,4),
(1,5);
INSERT INTO role_permissions (role_id, permission_id) VALUES
(2,4);




CREATE TABLE categories (
    id SERIAL PRIMARY KEY,
    category_name VARCHAR(255) NOT NULL,
    slug VARCHAR(255) UNIQUE,
    category_image TEXT,
    description TEXT,
    status BOOLEAN DEFAULT TRUE,
    is_deleted BOOLEAN DEFAULT FALSE,
	deleted_at TIMESTAMP NULL,
    created_by INTEGER,
    updated_by INTEGER,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
CREATE TABLE products (
    id SERIAL PRIMARY KEY,
    category_id INTEGER REFERENCES categories(id),
    product_name VARCHAR(255) NOT NULL,
    slug VARCHAR(255) UNIQUE,
    short_description TEXT,
    long_description TEXT,
    sku VARCHAR(100),
    barcode VARCHAR(100),
    price NUMERIC(10,2),
    discount_price NUMERIC(10,2),
    stock INTEGER DEFAULT 0,
    weight VARCHAR(50),
    unit VARCHAR(50),
    thumbnail_image TEXT,
    status BOOLEAN DEFAULT TRUE,
    is_featured BOOLEAN DEFAULT FALSE,
    is_deleted BOOLEAN DEFAULT FALSE,
	deleted_at TIMESTAMP NULL,
    created_by INTEGER,
    updated_by INTEGER,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE product_images (
    id SERIAL PRIMARY KEY,
    product_id INTEGER REFERENCES products(id) ON DELETE CASCADE,
    image_path TEXT,
    is_primary BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
CREATE TABLE category_images (
    id SERIAL PRIMARY KEY,
    catgory_id INTEGER REFERENCES categories(id) ON DELETE CASCADE,
    image_path TEXT,
	s_deleted BOOLEAN DEFAULT FALSE,
	deleted_at TIMESTAMP NULL,
    is_primary BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE product_meta (
    id SERIAL PRIMARY KEY,
    product_id INTEGER REFERENCES products(id) ON DELETE CASCADE,
    meta_title VARCHAR(255),
    meta_keywords TEXT,
    meta_description TEXT,
    canonical_url TEXT,
    schema_json JSONB,
    og_image TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE inventory (
    id SERIAL PRIMARY KEY,
    product_id INTEGER REFERENCES products(id) ON DELETE CASCADE,
    quantity INTEGER DEFAULT 0,
    min_stock INTEGER DEFAULT 0,
    max_stock INTEGER DEFAULT 0,
    stock_status VARCHAR(50),
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE product_reviews (
    id SERIAL PRIMARY KEY,
    product_id INTEGER REFERENCES products(id) ON DELETE CASCADE,
    customer_name VARCHAR(255),
    rating INTEGER,
    review TEXT,
    status BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

https://themeforest.net/item/larkon-ecommerce-management-admin-dashboard-template/53490068?srsltid=AfmBOormHYNKaeZDvOz8AATpoLTWDatdZxuSLLa8WcZYHC4TrY1t2-n4